<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Five-in-line Master</title>
    <link rel="stylesheet" type="text/css" href="css/style.css">
  </head>
  <body>
        <div id="wrap" style='display:none'></div>
        <div class='login'>
            <div class='loginName'>
              <% if(user && user.username){ %>
                   <header>
                      <h2><%= user.username %>, Ready to Start?</h2>
                      <input type="hidden" id="user" value=<%= user.username %>>
                      <input type="hidden" id="rank" value=<%= rank %>>
                      <input type="hidden" id="credit" value=<%= credit %>>
                   </header>
                   <table>
                     <tr>
                       <td>Your Current Rank: <%= rank %></td>
                     </tr>
                     <tr>
                       <td>Your Current Credit: <%= credit %></td>
                     </tr>
                     <tr>
                       <td style="color:red">Warn: Once started, you are not suggested to close the tab otherwise you will lose your credits.</td>
                     </tr>
                     <tr>
                       <td><input type="button" id="start" value="Start" onclick="renderBoard()">&nbsp &nbsp<a href="/index">Back</a></td>
                     </tr>
                   </table>
                   <% }else{ %>
                         <header>
                            <h2>Five-in-line Master</h2>
                            <h3>You have not logged in the system.</h3>
                            <a href="/">LogIn</a>
                            <a href="/register">Register</a>
                        </header>
                  <% } %>
            </div>
        </div>
    <script src="js/react.js"></script>
    <script src="js/react-dom.js"></script>
    <script src="../socket.io/socket.io.js"></script>
    <script src='dist/game.js'></script>
  </body>
</html>
